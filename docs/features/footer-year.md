# Footer year

**File:** `js/footer-date.js` (export: `footerDate`)

## What it does

Writes the current four-digit year into every `.current-year` element on the page, site-wide. The smallest feature in the codebase — no conditions beyond the elements existing.
